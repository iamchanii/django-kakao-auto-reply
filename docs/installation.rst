============
Installation
============

At the command line::

    $ easy_install django-rest-kakao-auto-reply

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-kakao-auto-reply
    $ pip install django-rest-kakao-auto-reply
